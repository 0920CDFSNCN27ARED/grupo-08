const CreateUpdateLayout = (props) => {
    return (
        <div id="CreateUpdateLayout" className="layouts">
            {props.children}
        </div>
    );
};

export default CreateUpdateLayout;
