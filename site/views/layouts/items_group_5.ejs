<!-- 
    Los banners recibirian el product id y el mismo seria utilizado
    para traer la informacion correspondiente del producto
    - imagen
    - nombre
    - precio
    - url

 -->

<section id="<%= bannerID %>" class="banner_items_group_5">
    <% for(let i = 0; i < featured_products.length; i++) { %> 

        <% if(i === 0) { %>
            <%- include('../components/banners/banner_square/index', {
                item: featured_products[i],
                isLarge: true
            })  %> 

        <% } else { %>
            <%- include('../components/banners/banner_square/index', {
                item: featured_products[i],
            })  %> 

        <% } %>   
        

    <% } %> 
</section>